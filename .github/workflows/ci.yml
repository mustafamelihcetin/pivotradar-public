name: CI

on:
  push:
    branches: []   # Hiçbir branch'te otomatik tetiklenme
  pull_request:
    branches: []

jobs:
  placeholder:
    name: CI (disabled for public mirror)
    runs-on: ubuntu-latest
    if: false        # Bu workflow çalışmaz — public referans repo
    steps:
      - run: echo "disabled"
